pipeline {
    agent any
    stages {
        stage('test') {
            steps {
                sh 'ruby --version'
                sh 'which ruby'
                sh 'echo $PATH'
            }
        }
    }
}
